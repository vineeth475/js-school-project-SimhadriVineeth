// src_filter/index.ts

import { initializeFilter } from './filter';

// Initialize the filter feature when the DOM is ready.
document.addEventListener("DOMContentLoaded", () => {
    initializeFilter();
});
